# SubSpace Developer Workflow

Visual guide to the SubSpace development workflow, from cloning to contributing.

## Table of Contents

- [Development Workflow Overview](#development-workflow-overview)
- [Build System Architecture](#build-system-architecture)
- [Debugging Workflow](#debugging-workflow)
- [Testing Workflow](#testing-workflow)
- [Contribution Workflow](#contribution-workflow)

---

## Development Workflow Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                     SubSpace Development Cycle                       │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │  Clone Repo  │
    └──────┬───────┘
           │
           ▼
    ┌──────────────────┐
    │ Init Submodules  │
    │  (artwork)       │
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │  Meson Setup     │
    │  (configure)     │
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │  Meson Compile   │
    │  (build)         │
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │   Test/Run       │
    │  ./subspace.py   │
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │  Make Changes    │
    │  (code/assets)   │
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │  Incremental     │
    │  Build           │
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │  Test Changes    │
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │  Commit & Push   │
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │  Pull Request    │
    └──────────────────┘
```

---

## Build System Architecture

### Component Overview

```
┌────────────────────────────────────────────────────────────────────┐
│                    SubSpace Build Components                        │
└────────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   C Source   │     │ Rust Source  │     │  Lua Scripts │
│  (src/*.c)   │     │  (src/*.rs)  │     │  (dat/*.lua) │
└──────┬───────┘     └──────┬───────┘     └──────┬───────┘
       │                    │                     │
       │                    │                     │
       ▼                    ▼                     │
┌──────────────┐     ┌──────────────┐           │
│  C Compiler  │     │    Cargo     │           │
│   (clang)    │     │  (rustc)     │           │
└──────┬───────┘     └──────┬───────┘           │
       │                    │                     │
       │                    │                     │
       ▼                    ▼                     │
┌──────────────┐     ┌──────────────┐           │
│ libsubspace.a│     │ libnaev.rlib │           │
│ (C library)  │     │(Rust library)│           │
└──────┬───────┘     └──────┬───────┘           │
       │                    │                     │
       └────────┬───────────┘                     │
                │                                 │
                ▼                                 │
         ┌──────────────┐                        │
         │   Linker     │                        │
         │   (meson)    │                        │
         └──────┬───────┘                        │
                │                                 │
                ▼                                 │
         ┌──────────────┐                        │
         │  subspace    │◄───────────────────────┘
         │ (executable) │     (loaded at runtime)
         └──────────────┘
```

### File Flow During Build

```
Source Tree                    Build Tree (builddir/)
───────────                    ──────────────────────

src/
├── naev.c                →    ├── subspace (executable)
├── naev.rs               →    ├── subspace.py (wrapper)
├── player.c              →    ├── naev.py (compat wrapper)
├── ...                   →    ├── libsubspace.a
└── core/                 →    ├── libnaev.rlib
    └── *.rs              →    └── meson_overlay.zip

dat/
├── missions/             →    dat/
│   └── *.lua             →    └── gettext/
├── outfits/              →        └── (translations)
│   └── *.xml             →
└── ships/                
    └── *.xml             

assets/ (submodule)       →    (loaded at runtime,
├── gfx/                  →     not copied to build)
└── snd/                  →

utils/build/
└── subspace.py           →    builddir/subspace.py
                               (configured with paths)
```

### Build Process Detail

```
┌──────────────────────────────────────────────────────────────────┐
│                 Detailed Build Process Flow                       │
└──────────────────────────────────────────────────────────────────┘

1. Meson Setup Phase
   ┌────────────────────────────────────────┐
   │ meson setup builddir                   │
   │                                        │
   │ • Detect compilers (clang, rustc)     │
   │ • Check dependencies (SDL3, etc.)     │
   │ • Generate build.ninja                │
   │ • Configure Cargo environment         │
   │ • Set up Rust target paths            │
   └────────────────────────────────────────┘
                    ↓

2. C Compilation Phase
   ┌────────────────────────────────────────┐
   │ Compile C sources                      │
   │                                        │
   │ src/*.c  → obj/*.o                     │
   │                                        │
   │ • Include headers                      │
   │ • Apply compiler flags                 │
   │ • Generate debug symbols (if debug)    │
   └────────────────────────────────────────┘
                    ↓
   ┌────────────────────────────────────────┐
   │ Create static library                  │
   │                                        │
   │ obj/*.o  → libsubspace.a               │
   └────────────────────────────────────────┘
                    ↓

3. Rust Compilation Phase
   ┌────────────────────────────────────────┐
   │ Cargo build (via meson)                │
   │                                        │
   │ src/*.rs  → libnaev.rlib               │
   │                                        │
   │ • Compile Rust library                 │
   │ • Link with C bindings (via bindgen)   │
   │ • Generate Rust dependencies           │
   └────────────────────────────────────────┘
                    ↓

4. Linking Phase
   ┌────────────────────────────────────────┐
   │ Link executable                        │
   │                                        │
   │ libsubspace.a + libnaev.rlib +         │
   │ dependencies → subspace                │
   │                                        │
   │ • Link system libraries                │
   │ • Apply linker flags                   │
   │ • Export symbols (if needed)           │
   └────────────────────────────────────────┘
                    ↓

5. Post-Build Phase
   ┌────────────────────────────────────────┐
   │ Generate auxiliary files               │
   │                                        │
   │ • Configure subspace.py wrapper        │
   │ • Build translation files (.gmo)       │
   │ • Create meson_overlay.zip             │
   │ • Generate AUTHORS file                │
   └────────────────────────────────────────┘
                    ↓
            ┌───────────────┐
            │ Build Complete │
            └───────────────┘
```

---

## Debugging Workflow

### VS Code Debug Flow

```
┌──────────────────────────────────────────────────────────────────┐
│              Visual Studio Code Debug Workflow                    │
└──────────────────────────────────────────────────────────────────┘

User Action              VS Code                   System
───────────              ───────                   ──────

Press F5          →   Read launch.json      →   
                        ↓
                  Check preLaunchTask       →   
                        ↓
                  Run "build-subspace"      →   meson compile
                        ↓                            ↓
                  Wait for build                 Building...
                        ↓                            ↓
                  ←────────────────────────────  Build done
                        ↓
                  Launch debugger           →   Start GDB/LLDB
                        ↓                            ↓
                  Set breakpoints           →   Install breakpoints
                        ↓                            ↓
                  Run program               →   Execute subspace
                        ↓                            ↓
                                                 ┌──────────────┐
[Breakpoint hit]  ←───────────────────────────  │  Program     │
                        ↓                        │  paused at   │
Show Variables    ←   Debugger interface         │  breakpoint  │
Call Stack        ←   ↓                          └──────────────┘
                        ↓
Step Over (F10)   →   Send step command     →   Execute line
                        ↓                            ↓
Update UI         ←───────────────────────────  Line executed
                        ↓
Continue (F5)     →   Send continue         →   Resume execution
                        ↓                            ↓
                                                 Running...
```

### Command-Line Debug Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                 Command-Line Debug Workflow                       │
└──────────────────────────────────────────────────────────────────┘

Terminal Session:

$ cd builddir
$ ./subspace.py
   ↓
Python wrapper starts
   ↓
Check for debugger (GDB/LLDB)
   ↓
Found GDB → Launch with GDB
   ↓
┌────────────────────────────────────┐
│ GNU gdb (GDB) 12.1                 │
│ (gdb) run                          │
│                                    │
│ Starting program: ./subspace       │
│ [Thread debugging using libthread]│
│ [New Thread 0x7ffff7fe9700 (LWP)] │
│                                    │
│ Program received signal SIGSEGV    │
│ 0x0000555555558a4d in player_init │
│ (gdb) backtrace                    │
│ #0  0x555555558a4d in player_init  │
│ #1  0x555555559123 in game_start   │
│ #2  0x55555555a456 in main         │
│ (gdb) print player                 │
│ $1 = (Player *) 0x0                │
│ (gdb) quit                         │
└────────────────────────────────────┘
```

### Breakpoint Workflow

```
Setting and Using Breakpoints
──────────────────────────────

1. Set Breakpoint
   ┌────────────────────────────┐
   │ Click margin in VS Code    │
   │ OR                         │
   │ (gdb) break player_init    │
   └────────┬───────────────────┘
            │
            ▼
   ┌────────────────────────────┐
   │ Red dot appears (VS Code)  │
   │ OR                         │
   │ Breakpoint 1 at 0x... (GDB)│
   └────────┬───────────────────┘
            │
            ▼
2. Run Program
   ┌────────────────────────────┐
   │ Press F5 (VS Code)         │
   │ OR                         │
   │ (gdb) run                  │
   └────────┬───────────────────┘
            │
            ▼
3. Program Executes
   ┌────────────────────────────┐
   │ Normal execution until...  │
   └────────┬───────────────────┘
            │
            ▼
4. Hit Breakpoint
   ┌────────────────────────────┐
   │ ⚠ Execution paused         │
   │ Line highlighted           │
   │ Variables visible          │
   └────────┬───────────────────┘
            │
            ▼
5. Inspect State
   ┌────────────────────────────┐
   │ • View variables           │
   │ • Check call stack         │
   │ • Evaluate expressions     │
   └────────┬───────────────────┘
            │
            ▼
6. Continue Execution
   ┌────────────────────────────┐
   │ F5: Continue               │
   │ F10: Step Over             │
   │ F11: Step Into             │
   │ Shift+F11: Step Out        │
   └────────────────────────────┘
```

---

## Testing Workflow

### In-Game Testing Process

```
┌──────────────────────────────────────────────────────────────────┐
│                    In-Game Testing Workflow                       │
└──────────────────────────────────────────────────────────────────┘

Feature: Testing Ship Flight Controls
──────────────────────────────────────

Step 1: Launch Game
   ┌─────────────────────┐
   │ $ ./subspace.py     │
   └──────┬──────────────┘
          │
          ▼
   ┌─────────────────────┐
   │ Game launches       │
   │ Main menu appears   │
   └──────┬──────────────┘
          │
          ▼
Step 2: Start New Game
   ┌─────────────────────┐
   │ Click "New Game"    │
   │ Select ship         │
   │ Choose start        │
   └──────┬──────────────┘
          │
          ▼
   ┌─────────────────────┐
   │ Game world loads    │
   │ Ship in space       │
   └──────┬──────────────┘
          │
          ▼
Step 3: Test Controls
   ┌─────────────────────────────┐
   │ Press W: Accelerate         │
   │ ✓ Ship moves forward        │
   │                             │
   │ Press A/D: Turn             │
   │ ✓ Ship rotates              │
   │                             │
   │ Press S: Brake              │
   │ ✓ Ship slows down           │
   │                             │
   │ Move mouse: Aim             │
   │ ✓ Ship faces direction      │
   └──────┬──────────────────────┘
          │
          ▼
Step 4: Log Results
   ┌─────────────────────────────┐
   │ ✓ All controls responsive   │
   │ ✓ No lag or stuttering      │
   │ ✓ Smooth rotation           │
   │ ✗ Deceleration too fast     │← Issue found!
   └──────┬──────────────────────┘
          │
          ▼
Step 5: File Bug/Fix
   ┌─────────────────────────────┐
   │ Create GitHub issue         │
   │ OR                          │
   │ Fix in code and retest      │
   └─────────────────────────────┘
```

### Test Checklist Matrix

```
┌──────────────────────────────────────────────────────────────────┐
│                     Testing Checklist                             │
└──────────────────────────────────────────────────────────────────┘

Category        Feature              Status    Notes
────────        ───────              ──────    ─────
Core
  ├─ Launch     Start game           [ ]       
  ├─ Menu       Navigation           [ ]       
  ├─ Save       Save/Load            [ ]       
  └─ Exit       Quit cleanly         [ ]       

Gameplay
  ├─ Flight     Movement             [ ]       
  ├─ Combat     Weapons/damage       [ ]       
  ├─ Landing    Spaceports           [ ]       
  ├─ Trading    Buy/sell goods       [ ]       
  └─ Missions   Accept/complete      [ ]       

Graphics
  ├─ Rendering  Ships/planets        [ ]       
  ├─ Effects    Explosions           [ ]       
  ├─ UI         Menus/HUD            [ ]       
  └─ Perf       60+ FPS              [ ]       

Audio
  ├─ Music      Background music     [ ]       
  ├─ SFX        Sound effects        [ ]       
  └─ Volume     Settings work        [ ]       

Network (Future)
  ├─ Connect    Join server          [N/A]     Not implemented
  ├─ Sync       State sync           [N/A]     
  └─ Chat       Communication        [N/A]     
```

---

## Contribution Workflow

### Fork and PR Process

```
┌──────────────────────────────────────────────────────────────────┐
│               GitHub Contribution Workflow                        │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│ Original Repo                │
│ shifty81/naev-mine           │
│ (main branch)                │
└────────────┬─────────────────┘
             │
             │ Fork
             ▼
┌──────────────────────────────┐
│ Your Fork                    │
│ username/naev-mine           │
│ (main branch)                │
└────────────┬─────────────────┘
             │
             │ Clone
             ▼
┌──────────────────────────────┐
│ Local Repository             │
│ ~/subspace                   │
└────────────┬─────────────────┘
             │
             │ Create branch
             ▼
┌──────────────────────────────┐
│ Feature Branch               │
│ feature/my-awesome-feature   │
└────────────┬─────────────────┘
             │
             │ Make changes
             ▼
┌──────────────────────────────┐
│ Edit Code                    │
│ • Modify files               │
│ • Add tests                  │
│ • Update docs                │
└────────────┬─────────────────┘
             │
             │ Test locally
             ▼
┌──────────────────────────────┐
│ Build & Test                 │
│ meson compile                │
│ ./subspace.py                │
└────────────┬─────────────────┘
             │
             │ Commit
             ▼
┌──────────────────────────────┐
│ Git Commit                   │
│ git add .                    │
│ git commit -m "Add feature"  │
└────────────┬─────────────────┘
             │
             │ Push
             ▼
┌──────────────────────────────┐
│ Push to Fork                 │
│ git push origin feature/...  │
└────────────┬─────────────────┘
             │
             │ Create PR
             ▼
┌──────────────────────────────┐
│ Pull Request                 │
│ github.com → New PR          │
└────────────┬─────────────────┘
             │
             │ Review
             ▼
┌──────────────────────────────┐
│ Code Review                  │
│ • Comments                   │
│ • Requested changes          │
│ • Approval                   │
└────────────┬─────────────────┘
             │
             │ Merge
             ▼
┌──────────────────────────────┐
│ Merged to Main               │
│ ✓ Feature shipped!           │
└──────────────────────────────┘
```

### Commit Message Flow

```
Good Commit Messages
────────────────────

Format:
[Type] Brief description (50 chars)

Detailed explanation (wrap at 72 chars)
- Why this change was needed
- What approach was taken
- Any side effects or considerations

Example:

┌────────────────────────────────────────────────────────┐
│ [Feature] Add shield regeneration mechanic            │
│                                                        │
│ Implements shield regeneration for all ships based    │
│ on their outfit configuration. Shields now regen at   │
│ configurable rates when not taking damage.            │
│                                                        │
│ - Added shield_regen field to ship stats             │
│ - Modified pilot_update() to process regen           │
│ - Added visual indicator for shield charging         │
│                                                        │
│ Closes #123                                           │
└────────────────────────────────────────────────────────┘

Types: [Feature], [Bugfix], [Refactor], [Docs], [Test]
```

---

## Quick Reference Commands

```
┌──────────────────────────────────────────────────────────────────┐
│                    Command Reference                              │
└──────────────────────────────────────────────────────────────────┘

Initial Setup
─────────────
git clone https://github.com/shifty81/naev-mine.git subspace
cd subspace
git submodule update --init --recursive
meson setup builddir --buildtype=debug

Build Commands
──────────────
cd builddir
meson compile                    # Build everything
meson compile subspace           # Build only executable
meson compile -j $(nproc)        # Parallel build

Run Commands
────────────
./subspace.py                    # Run with wrapper
./subspace                       # Run directly
WITHDEBUGGER=false ./subspace.py # No debugger

Clean/Rebuild
─────────────
rm -rf builddir                  # Full clean
meson setup builddir             # Reconfigure
meson compile --clean            # Clean build artifacts

Debug Commands
──────────────
gdb ./subspace                   # Debug with GDB
lldb ./subspace                  # Debug with LLDB
code .                           # Open in VS Code

Git Commands
────────────
git checkout -b feature/my-feat  # Create branch
git add .                        # Stage changes
git commit -m "Description"      # Commit
git push origin feature/my-feat  # Push to fork
```

---

## Summary

This workflow guide covered:
- ✅ Complete development cycle
- ✅ Build system architecture
- ✅ Debugging workflows
- ✅ Testing procedures
- ✅ Contribution process

**Next steps:**
- Read [BUILD_GUIDE.md](BUILD_GUIDE.md) for detailed instructions
- Check [CONTRIBUTING_SUBSPACE.md](CONTRIBUTING_SUBSPACE.md) for guidelines
- Explore [ARCHITECTURE.md](ARCHITECTURE.md) for technical details

**Happy developing! 🚀**

*SubSpace Development Team*
